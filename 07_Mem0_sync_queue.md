# Mem0 sync queue

Дата создания: 2026-04-14
Статус: pending / local placeholder until real Mem0 integration exists

## Зачем этот файл

Сейчас в проекте не найден живой подключённый `Mem0` backend/config, поэтому сюда складываются записи, которые пользователь просит **сохранить в Mem0 потом**, когда реальная интеграция будет подключена.

Это не замена Mem0, а честная очередь на будущую синхронизацию.

---

## 2026-04-14 — сохранить точку остановки исследования памяти в будущий Mem0

### Что сохранить
- Исследование памяти `НоваяПамятьV3` уже собрано в сильную первую версию, но ещё не финализировано.
- Следующий порядок работ зафиксирован так:
  1. пользователь докидывает новые ссылки;
  2. обновляется `01_Большое_исследование_memory_инструментов_2026.md`;
  3. потом повторно дочищается `02_Карта_решений_что_берём_что_нет.md`;
  4. только после этого пользователь смотрит `03_Roadmap_Памяти_v3.md`.
- Отдельная точка остановки сохранена в:
  - `C:\Progi\VibeProjects\Openclaw\НоваяПамятьV3\06_Прогресс_исследования_и_точка_остановки.md`

### Тип памяти
- project memory
- research status
- stop-point / next-step memory

### Почему важно
- чтобы не потерять, что roadmap пока не финализируем;
- чтобы после новых ссылок не перескакивать сразу к roadmap;
- чтобы помнить, что сейчас главный фокус — добивка research и decision layer.
